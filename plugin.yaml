# plugin.yaml
name: hermes-livekit
label: LiveKit WebRTC
kind: platform
version: 1.8.0
description: Hermes-native voice, vision, and MiRA context transport over LiveKit
author: kortexa-ai

# MiRA's itinerary, location, and meeting-transcript context is served by the
# hermes-mira-context MCP server (services/hermes-mcp) instead of plugin-
# declared tools — see agents/hermes-livekit/configure_yaml.py for how it
# gets registered in mcp_servers.

provides_hooks:
  - post_api_request
  - on_session_finalize

requires_env:
  - name: LIVEKIT_URL
    description: "The WebSocket URL for your LiveKit server"
    prompt: "LiveKit URL (e.g., wss://your-instance.livekit.cloud)"
    password: false
  - name: LIVEKIT_API_KEY
    description: "Your LiveKit Project API Key"
    prompt: "LiveKit API Key"
    password: false
  - name: LIVEKIT_API_SECRET
    description: "Your LiveKit Project API Secret"
    prompt: "LiveKit API Secret"
    password: true # Hides the input in the CLI UI
